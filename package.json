{
  "name": "@borilla/base64-thumbnail",
  "version": "0.2.0",
  "author": "borilla",
  "description": "Create thumbnail images and return as base64 encoded jpeg",
  "keywords": [
    "base64",
    "image",
    "thumbnail"
  ],
  "license": "MIT",
  "repository": "borilla/base64-thumbnail",
  "main": "index.js",
  "bin": {
    "write-thumbnails-json": "cli.js"
  },
  "scripts": {
    "build": "echo 'build'",
    "test": "npm run unit-test && npm run lint",
    "unit-test": "mocha test",
    "lint": "eslint src test",
    "prebuild": "npm test",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "commander": "^2.9.0",
    "glob": "^7.1.1",
    "jimp": "^0.2.27"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-subset": "^1.5.0",
    "eslint": "^2.13.1",
    "mocha": "^2.5.3",
    "sinon": "^2.1.0",
    "sinon-chai": "^2.9.0"
  }
}
